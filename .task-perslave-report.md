# Per-Slave Amp Cap — Task Report

**Status:** Complete

**Commit:** ae8fa82 — `feat: per-slave amp cap via chargectl/control/slave/<twc>/max_amps`

**Tests:** 58 passed, 0 failed (4 new tests added: `test_allocate_respects_charging_cap`, `test_allocate_caps_never_exceed_desired`, `test_set_slave_cap_and_default`, `test_per_slave_control_topic_routes_correctly`)

**Breaker math (`calculate`):** Untouched — no changes were made to the `calculate` method in `modulation.py`.
